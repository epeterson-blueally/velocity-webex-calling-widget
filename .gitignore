node_modules/
dist/
*.log
.DS_Store
.env
.env.*
# Cloudflare Worker local secrets — NEVER commit
.dev.vars
**/.dev.vars
.wrangler/
# Pilot config (client id / worker url / tenant values) — kept out of git; goes in the layout JSON at Phase 7
PILOT-CONFIG.local.md
coverage/
# cloned Cisco samples live under /reference (~249M); NOT committed per BUILD-PLAN §1
# ("git-ignored or submodule-less copies"). The durable extracted patterns live in DISCOVERY.md.
# Clones remain on disk for later phases' subagents to read.
reference/
